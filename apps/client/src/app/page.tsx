'use client';

import Header from '@/components/common/Header';
import React from 'react';

const page = () => {
	return (
		<div style={{ height: '300vh' }}>
			<Header />
		</div>
	);
};

export default page;
