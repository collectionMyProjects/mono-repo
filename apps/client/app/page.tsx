import React from 'react';
import { vars } from '@with/foundation';

const page = () => {
	return (
		<div
			style={{
				width: '100%',
				height: '100vh',
				backgroundColor: vars.colors.green[600],
			}}
		></div>
	);
};

export default page;
